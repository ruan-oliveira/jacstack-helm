apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "theia-ruan.fullname" . }}-test-connection"
  labels:
    {{- include "theia-ruan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "theia-ruan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
